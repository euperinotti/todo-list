import { CheckTaskProps } from "./types"

export const CheckTask = ({}: CheckTaskProps) => {
  return (<input type="checkbox" className="appearance-none text-red-500 red-500 border border-slate-700 rounded-full w-7 h-7"/>)
}